using System;
using System.Text.RegularExpressions;

namespace NummernValidator
{
    class Validator
    {
        string nummer;
        public Validator(int nummer) {
            //this.nummer = nummer;
        }

        //public int ziffernAnzahl(String nummer) {
        //    int anzahl = 0;
        //    for (int stringlaenge = nummer.Length - 1; stringlaenge > 0; stringlaenge--) {
        //        if (Char.IsDigit(nummer[stringlaenge])) anzahl++;
        //    }
        //    return anzahl;
        //}

        //public Boolean enthaeltVerbotenesZeichen(String nummer) {
        //    string pat = @"[^\\d\\]";
        //}

    }
}